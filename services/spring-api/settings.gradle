pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        maven { url = uri("https://repo.spring.io/plugins-release") }
    }
}

rootProject.name = 'niche-explorer'

include 'api-server'

project(':api-server').projectDir = file('src/api-server')
